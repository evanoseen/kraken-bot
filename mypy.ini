[mypy]
# Per-file typing rollout: each module is typed on its own daily-iteration day
# (Day 15 kraken_client, Day 16 trader, …). To keep each day self-contained,
# mypy reads transitive imports for inference but does not report errors in
# them. The day for each module fixes its own errors.
follow_imports = silent
ignore_missing_imports = True
